"use strict";

/**
 * Module: config.js
 * Type: Config
 * Purpose: Constants, URLs, feature flags.
 *
 * Depends on: nothing
 * Used by: effect modules, main.js
 * Side effects: None
 */

export const CONFIG = Object.freeze({
  TRANSLATIONS_BASE: "/translations",
  CHATBOT_IFRAME_URL: "http://localhost:8267/rpm-chatbot.html",
});
