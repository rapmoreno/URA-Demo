/**
 * Stub for talkinghead - used only when Vite scans chatbot code.
 * The chatbot runs on its own server and uses the real talkinghead via CDN import map.
 */
export const TalkingHead = class {};
export default { TalkingHead };
